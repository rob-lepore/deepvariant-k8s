apiVersion: v1
kind: Pod
metadata:
  name: nextflow-hello
spec:
  restartPolicy: Never
  volumes:
    - name: shared
      persistentVolumeClaim:
        claimName: shared-pvc
  containers:
    - name: nextflow
      image: nextflow/nextflow:latest
      command: ["/bin/sh", "-c"]
      args:
        - "cd /mnt/shared && nextflow run hello"
      volumeMounts:
        - name: shared
          mountPath: /mnt/shared
